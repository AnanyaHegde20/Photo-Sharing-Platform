export const metadata = {
  title: "Create Account",
  description: "Create a PhotoShare account to join your team.",
};

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
