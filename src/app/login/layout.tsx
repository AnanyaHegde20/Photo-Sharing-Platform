export const metadata = {
  title: "Sign In",
  description: "Sign in to your PhotoShare account.",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
